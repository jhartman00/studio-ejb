# Admin auth
ADMIN_PASSWORD=
ADMIN_SESSION_SECRET=

# Vercel Postgres / Neon (auto-injected when linked to project)
DATABASE_URL=
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Vercel Blob (auto-injected when linked)
BLOB_READ_WRITE_TOKEN=
BLOB_STORE_ID=
BLOB_WEBHOOK_PUBLIC_KEY=

# Vercel KV (rate limiting). Falls back to in-memory if missing.
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Email
RESEND_API_KEY=
RESEND_FROM=hello@studioejb.com
EMMA_EMAIL=

# CAN-SPAM postal address. Campaigns cannot send until this is set to a real address.
STUDIO_MAILING_ADDRESS=
